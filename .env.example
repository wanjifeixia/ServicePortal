# 必填：用于授权执行服务器更新。请使用高强度随机值，不要提交真实值。
SERVICEPORTAL_ADMIN_TOKEN=replace-with-a-long-random-token

# 可选：提高 GitHub API 限额；只在服务器环境中设置。
GITHUB_TOKEN=

# 部署到服务器时可改为 0.0.0.0，并通过 Nginx/Caddy 提供 HTTPS。
HOST=127.0.0.1
PORT=4173
